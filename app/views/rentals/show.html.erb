<div class="container p-3 mt-3">
  <div class="row justify-content-center">
    <div class="col-lg-6 shadow rounded-3 mb-3 show-card">
      <h2 class="py-3 text-center">Ma location de <%= @rental.game.name %> 🥳</h2>
      <div class="d-flex justify-content-between pb-2"><p>Durée: </p><span><%= @rental.start_date %> au <%= @rental.end_date %></span></div>
      <div class="d-flex justify-content-between pb-2"><p>Prix: </p><span><%= @rental.price %> €</span></div>
      <div class="d-flex justify-content-between pb-2"><p>Lieu: </p><span><%= @rental.game.address %></span></div>
      <div class="d-flex justify-content-between pb-2"><p>Propriétaire à contacter:</p></div>
      <div class="p-3 mb-3 shadow rounded-3 owner-card">
        <div class="d-flex justify-content-center"><p>Pseudo: <%= @rental.game.user.username %></p></div>
        <div class="d-flex justify-content-center"><p>Email: <%= @rental.game.user.email %></p></div>
        <div class="d-flex justify-content-center"><p>Num: <%= @rental.game.user.phone_number %></p></div>
      </div>
    </div>
    <%= link_to "< Mes locations", rentals_path %>
  </div>
</div>
